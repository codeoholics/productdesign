function displayProjectDetails(projectId) {
    // Redirect to a new page for the selected project
    window.location.href = projectId + '.html';
}

function display(sectionId) {
    // You can add logic here to navigate to the home section or perform other actions.
    // For simplicity, let's just log the section id for now.
    console.log('Navigating to:', sectionId);
}